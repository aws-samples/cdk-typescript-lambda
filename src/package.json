{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc && npm-run-all build-*",
    "build-addition": "cd Add && npm run build",
    "build-multiply": "cd Multiply && npm run build",
    "package": "npm-run-all pk-*",
    "pk-addition": "cd Add && npm run package",
    "pk-multiply": "cd Multiply && npm run package"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
